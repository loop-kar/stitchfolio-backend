# Copy to .env on the server (do not commit .env).
# The workflow writes ENV_FILE_PATH, CONFIG_FILE, NGINX_HTTP_PORT, NGINX_HTTPS_PORT.
# Add SSL by creating .env.ssl in the deploy dir (see DEPLOYMENT.md).

# Dev (e.g. in /stitchfolio/backend-dev):
# ENV_FILE_PATH=/root/stitchfolio_env/dev.env
# CONFIG_FILE=dev.yaml
# NGINX_HTTP_PORT=9001
# NGINX_HTTPS_PORT=9002
# SSL_CERT_DIR=/etc/letsencrypt/live/dev-api.example.com   # in .env.ssl

# Prod (e.g. in /stitchfolio/backend-prod):
# ENV_FILE_PATH=/root/stitchfolio_env/prod.env
# CONFIG_FILE=prod.yaml
# NGINX_HTTP_PORT=80
# NGINX_HTTPS_PORT=443
# SSL_CERT_DIR=/etc/letsencrypt/live/api.example.com   # in .env.ssl

ENV_FILE_PATH=/root/stitchfolio_env/prod.env
CONFIG_FILE=prod.yaml
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
SSL_CERT_DIR=/etc/letsencrypt/live/api.example.com
