# Copy to .env on the server (do not commit .env).
# Dev (e.g. in /stitchfolio/backend-dev):
# ENV_FILE_PATH=/stitchfolio/env/.dev.env
# CONFIG_FILE=dev.yaml
# NGINX_HOST_PORT=9001
#
# Prod (e.g. in /stitchfolio/backend-prod):
# ENV_FILE_PATH=/stitchfolio/env/.prod.env
# CONFIG_FILE=prod.yaml
# NGINX_HOST_PORT=80

ENV_FILE_PATH=/stitchfolio/env/.prod.env
CONFIG_FILE=prod.yaml
NGINX_HOST_PORT=80
